#include <stdio.h>

int main(){
    unsigned int a=0x12345678;
    a |= (1<<12); // 13位置1
    a &= ~(1<<12); // 13位清0
    a ^= (1<<12); // 13位反转
    a = (a & (~((1<<25)+(1<<26)))) + (1<<25); // 26位置1 27位清0
    return 0;
}